# Agent catalog

Agent definitions are product contracts, not runtime code. Each definition describes a narrow responsibility, allowed tools, input/output schema, escalation behavior, and safety constraints.

The first planned specialists are discovery, local expert, social curator, and trip concierge. Runtime adapters live in `apps/api/app/agents`.
